<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">


    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a title="Go Home" class="navbar-brand" href="/"><i class="glyphicon glyphicon-tower"></i>  Captured</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "All Users", users_path %></li>
        <li><%= link_to "All Galleries", galleries_path %></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <!-- <li>
          <form class="navbar-form" role="search">
          <div class="input-group">
              <input type="text" class="form-control" placeholder="Search" name="q">
              <div class="input-group-btn">
                  <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
              </div>
          </div>
          </form>
        </li> -->
        <% if current_user %>
          <li><%= link_to "Signed in as #{current_user.username}", user_path(current_user) %></li>
          <li><div class="btn-group btn-group-md">

            <%= link_to  user_path(current_user),title: "My Galleries", class: "btn btn-primary navbar-btn" do%>
                <i class="glyphicon glyphicon-th"></i>
            <% end %>
            <%= link_to 'Signout', logout_path, method: :delete, class: "btn btn-info navbar-btn" %>
          </div></li>
        <% else %>
          <li><div class="btn-group btn-group-md">
            <%= link_to 'Sign Up', new_user_path, class: "btn btn-primary navbar-btn" %>
            <%= link_to 'Sign In', login_path, class: "btn btn-info navbar-btn" %>
          </div></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>
<% flash.each do |k,v|%>
  <div class="alert alert-<%= k %> text-center" role="alert">
    <p><%= v %></p>
  </div>
<% end if flash.any? %>
