<h1>Users:</h1>
<hr>

<div class="row text-center">
  <% @users.each do |u|%>
    <div class="col-xs-4 col-sm-2 col-lg-1 text-center">
      <%= link_to u.username, user_path(u), class: "btn btn-success" %>
    </div>

  <% end %>
</div>
